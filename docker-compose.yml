version: "3.8"
services:
  database:
    image: "postgres:latest"
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: jdr
      POSTGRES_PASSWORD: password
      POSTGRES_DB: jdr
    volumes:
      - ./db-data/:/var/lib/postgresql/data/